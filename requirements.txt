# Core web framework
Flask>=3.0,<4.0
# Template engine (Flask depends on this but pin explicitly)
Jinja2>=3.1,<4.0
# Optional data handling (you can remove if not needed)
pandas>=2.0,<3.0

# Production server
gunicorn~=21.0

# Database support
Flask-SQLAlchemy~=3.0
Flask-Migrate~=4.0

# AI Chatbot
openai>=1.0.0
python-dotenv~=1.0
